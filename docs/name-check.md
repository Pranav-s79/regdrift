# Name availability check (Sprint 0, Task 1)

Checked 2026-07-06.

## regdrift

- **PyPI**: free — `https://pypi.org/pypi/regdrift/json` returns 404 (no package registered).
- **GitHub**: `Pranav-s79/regdrift` already created (empty) and in use as this repo's origin.

**Verdict: name is available. Proceeding as `regdrift`.**

## Backups (both verified free on PyPI, same date)

1. `svd-drift` — keeps the "drift" framing, adds the file format for discoverability.
2. `regdiff` — plainer, reads as "register diff"; slightly less distinctive.
